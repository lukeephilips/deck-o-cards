require 'deck'
require 'rspec'

describe Deck do
  # before do
  #   @new_deck = Deck.new
  # end
  describe '#init'
    it "creates a deck" do
    #   expect(@new_deck.cards.length).to eq(52)
    end
  end
end
